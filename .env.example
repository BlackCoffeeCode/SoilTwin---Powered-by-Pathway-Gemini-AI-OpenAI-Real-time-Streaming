# =============================================================================
# SoilTwin — Environment Variables Template
# Copy to .env and fill in your values. Never commit .env to version control.
# =============================================================================

# ── LLM Provider Selection ────────────────────────────────────────────────────
# Which LLM to use for Advisory Chat:  openai | gemini | claude
# If the selected provider fails, the system auto-falls-back to the next available.
LLM_PROVIDER=openai

# ── OpenAI (Required if LLM_PROVIDER=openai or as fallback) ──────────────────
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx

# ── Google Gemini (Required if LLM_PROVIDER=gemini) ──────────────────────────
# Also used for Gemini embeddings in the RAG pipeline
GOOGLE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Anthropic Claude (Required if LLM_PROVIDER=claude) ───────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Weather API (Optional — falls back to simulated data if missing) ──────────
OPENWEATHER_API_KEY=your_openweather_api_key

# ── JWT Authentication ────────────────────────────────────────────────────────
# CHANGE THIS in production! Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=development_secret_key_change_in_production_123

# ── MongoDB ───────────────────────────────────────────────────────────────────
# Local:          mongodb://localhost:27017/soiltwin_db
# Docker Compose: mongodb://mongo:27017/soiltwin_db
# MongoDB Atlas:  mongodb+srv://<user>:<pass>@cluster.mongodb.net/soiltwin_db
MONGODB_URL=mongodb://localhost:27017/soiltwin_db
MONGODB_DB_NAME=soiltwin_db

# ── AWS S3 (Optional — for soil report PDF uploads) ───────────────────────────
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
S3_BUCKET_NAME=soiltwin-uploads
S3_REGION=ap-south-1

# ── Infrastructure ────────────────────────────────────────────────────────────
# Used by docker-compose.yml to replace hardcoded EC2 IP
API_HOST=localhost

# ── External Data (Optional) ─────────────────────────────────────────────────
# data.gov.in API key for OGD agricultural dataset proxy
DATA_GOV_IN_API_KEY=your_data_gov_in_api_key
